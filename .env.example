# Supabase Configuration (PUBLIC publishable keys only - safe for frontend)
VITE_SUPABASE_URL=https://ztrskyefkugevypzfecl.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-publishable-anon-key-here

# Corporate Email Domain (only emails from this domain can register/login)
VITE_ALLOWED_EMAIL_DOMAIN=immensesmartsolutions.com

# ==============================================================================
# SECURITY NOTES:
# 1. NEVER put the Supabase service_role / secret key here.
# 2. In Vercel Project Settings -> Environment Variables, add these 3 variables.
# 3. Row Level Security (RLS) protects all database tables and storage objects.
# 4. Credential encryption/decryption is handled server-side in PostgreSQL.
# ==============================================================================
